{
  "host": "${process.env.HOST || '0.0.0.0'}",
  "port": "${process.env.VIRTUAL_PORT || 2000}",
  "production": true,
  "proxy": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "path": "/dash",
    "autoOpen": false
  }
}
