version: '3.3'
services:
  strapi_staging:
    build:
      context: .
      args: 
        NODE_ENV: ${NODE_ENV}
        PORT: ${VIRTUAL_PORT}
    ports:
      - '127.0.0.1:1337:1337'
    volumes:
      - 'strapi_staging_public:/app/public'
    env_file: 
      .env
    networks:
      - backend
    external_links:
      - mariadb_server:mysql
    restart: unless-stopped
volumes:
  staging_public_files:
    external: true
networks:
  backend:
    external:
      name: backend