version: '3.3'
services:
  vue_staging:
    # name: 'frontend-staging.danieldevine.dev'
    build:
      context: .
    ports:
      - '127.0.0.1:8080:8080'
    env_file: 
      .env
    restart: unless-stopped
    networks:
      - backend
networks:
  backend:
    external:
      name: backend