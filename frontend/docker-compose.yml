version: '3.3'
services:
  vue_production:
    # name: 'frontend-production.danieldevine.dev'
    build:
      context: .
      args: 
        NODE_ENV: ${NODE_ENV}
    ports:
      - '8081:8080'
    env_file: 
      .env
    restart: unless-stopped
